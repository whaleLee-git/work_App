# Changelog

## 2026-03-16
- Added `sub-agents.yaml` with 8-agent team topology.
- Added collaboration guide and baseline docs templates under `docs/`.
- Added realtime dashboard (`dashboard/server.js`) with SSE updates for agent tasks and project progress.
- Added dashboard UI files (`dashboard/public/*`) and initial status data (`dashboard/data/status.json`).
